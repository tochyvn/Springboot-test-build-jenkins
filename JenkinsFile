pipeline {

    agent any

    stages {

    stage('Build') {
	    steps {
    	    script {
		 image_name = "overview-project"
		 archive_name = "${image_name}.tgz"
                 sh "sudo ls -la"
                 sh "java -version"
		 sh "sudo mvn --version"
		
	        }
   	        
        }
    }

    stage('Clean') {
	 steps {
		script {
			sh "sudo ls -la"
			}
		}
	}
}
    post {
	always {
		cleanWs()
		}
	}
}
