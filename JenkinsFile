pipeline {

    agent any

    stages {

    stage('Build') {
	    steps {
    	    script {

                 sh "sudo ls -la"
                 sh "java --version"
		         sh "sudo docker ps"

	        }
   	        archiveArtifacts artifacts: "${archive_name}"
        }
    }

    stage('Clean') {
	 steps {
		script {

			}
		}
	}
}
    post {
	always {
		cleanWs()
		}
	}
}
